enum AppEnv { production, staging }

enum AppLanguage { en, ar }

enum OnboardingStateEnum { login, signup, phone }

enum LocalKeys {
  uid,
  name,
  email,
  phone,
  referralCode,
  totalBalance,
  totalSessions,
  totalReferrals,
  earnedFromReferrals,
  userReferralCode,
  createdAt,
}
